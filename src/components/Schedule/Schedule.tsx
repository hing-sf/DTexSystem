import { Typography } from '@mui/material';
import { FC } from 'react';
import { StyledBox } from '../../pages/styles';

 const Schedule: FC = () => {
	return (
		<StyledBox>
			<Typography variant='h6' component='p'>
				Schedule Dummy component
			</Typography>
		</StyledBox>
	);
}

export default Schedule;